# https://lightning.ai/docs/pytorch/stable/api/lightning.pytorch.callbacks.ModelCheckpoint.html

model_checkpoint:
  _target_: lightning.pytorch.callbacks.ModelCheckpoint
  dirpath: null # directory to save the model file
  filename: "epoch_{epoch:03d}-val_auroc_{val/auroc_macro:.4f}" # Optional: clean naming scheme
  monitor: "val/auroc_macro" # Changed from null to track your validation AUROC
  verbose: False # verbosity mode
  save_last: True # Recommended: safe fallback to resume training if interrupted
  save_top_k: 1 # save k best models (determined by above metric)
  mode: "max" # Changed from "min" to "max" because higher AUROC is better
  auto_insert_metric_name: True # when True, the checkpoints filenames will contain the metric name
  save_weights_only: False # if True, then only the model’s weights will be saved
  every_n_train_steps: null # number of training steps between checkpoints
  train_time_interval: null # checkpoints are monitored at the specified time interval
  every_n_epochs: null # number of epochs between checkpoints
  save_on_train_epoch_end: null # whether to run checkpointing at the end of the training epoch or the end of validation